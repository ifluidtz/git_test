this is a git usage test
another test to know modification
git status check test
you can use git diff find out specific modification operation
commit next time
and use git reset --hard HEAD^ back to former version
test that add before commit will commit modification
but modification will not be record if commit directly
aaaaa
